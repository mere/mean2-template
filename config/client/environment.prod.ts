// CLIENT-SIDE CONFIGURATION: Production
// 
// These settings are copied to 'src/app/environment.ts' from 'config/client/environment.prod.ts' 
// during a production build.

export const environment = {
    production: true,
    apiUrl: '/api/'
};
